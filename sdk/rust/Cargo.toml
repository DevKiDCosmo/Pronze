[package]
name = "pronmf"
version = "0.1.0"
edition = "2021"
authors = ["PronKern Team"]
description = "Rust FFI bindings for PronKern SDK"

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1.0"
