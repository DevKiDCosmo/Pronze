[package]
name = "pronmftd"
version = "0.1.0"
edition = "2021"
authors = ["PronKern Team"]
description = "Pron OS Runtime Daemon (pronmftd)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.35", features = ["full"] }
